version: '2'
services:
  proxy:
    build: .
    restart: always
    ports:
      - 80:80
    networks:
      - quotster_default
      - svglinks_default
networks:
  quotster_default:
    external: true
  svglinks_default:
    external: true
